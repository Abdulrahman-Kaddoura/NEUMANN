services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: neumann
      POSTGRES_PASSWORD: neumann
      POSTGRES_DB: neumann
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
